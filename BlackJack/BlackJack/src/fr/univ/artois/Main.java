package fr.univ.artois;

public class Main {
	
	public static void main(String[] args){
		
		FenetrePrincipale fen = new FenetrePrincipale();
		
		/*DeckDeCartes deck = new DeckDeCartes();
		for(int i = 0; i < deck.getDeck().size(); ++i){
			System.out.println(deck.getDeck().get(i).getIndice());
		}
		*/
		
		/*String test = "Coucou tout le monde !";
		byte[] data = test.getBytes();
		try {
		   InetAddress adresse = InetAddress.getByName("localhost");
		   int port = 1664;
		   DatagramSocket ds = new DatagramSocket(port, adresse);
		   DatagramPacket dp = new DatagramPacket(data, data.length, adresse, port);
		   ds.send(dp);
		}catch (IOException ex){ }
		*/
		
	}

}
